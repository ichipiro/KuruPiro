name = "kurupiro"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# 本番環境用のService Bindings
[[services]]
binding = "API"
service = "kuru-piro-worker"

# Preview環境も一時的に本番Workerを参照
[[env.preview.services]]
binding = "API"
service = "kuru-piro-worker"
